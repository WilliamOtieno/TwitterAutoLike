- The prequisite command that you have to run in your terminal is:
        ```
        pip install -r requirements.txt

        ```

- Make sure you have a twitter developers account in order to be given your own authorization token keys
